<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Chats</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.6/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-4Q6Gf2aSP4eDXB8Miphtr37CMZZQ5oXLH2yaXMJ2w8e2ZtHTl7GptT4jmndRuHDT" crossorigin="anonymous">
    <link rel="stylesheet" href="/style.css">
</head>

<body>
    <h4 class="pt-3">-NODE WITH MONGODB-</h4>
    <p class="text-muted mb-5">Basic operation on Restful API's using chat post. <a
            href="http://localhost:8080/chats/new">Create Chat</a></p>
    <% for(data of chats) {%>
        <div class="chats">
            <p class="text-muted">From: <i>
                    <%= data.from %>
                </i>
            </p>
            <p class="msg"><span>
                    <%= data.msg %>
                </span>
            </p>
            <p class="text-muted">Received By: <i>
                    <%= data.to %>
                </i>
            </p>

            <hr>
            <p class="text-muted">created_at:
                <i>
                    <%= data.created_at.toString().split(" ")[4] %>
                    <%= data.created_at.toString().split(" ").splice(0,4).join(" ") %>
                </i>
            </p>
            
            <div class=" btns">
                        <form action="/chats/<%= data.id %>/edit" method="GET">
                            <button class="m-1 btn btn-warning">Edit</button>
                        </form>
                        <form action="/chats/<%= data.id %>?_method=DELETE" method="POST">
                            <button class="m-1 btn btn-danger">Delete</button>
                        </form>
        </div>
        </div>
        <% } %>
</body>

</html>